// const { add } = require("./utils/add.js");

// const { subtract } = require("./utils/subtract.js");

// const { add, subtract } = require("./utils/index.js");

const { add, subtract } = require("./utils");

console.log(add(40, 60));

console.log(subtract(100, 20));
